spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:planbookai}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_PASSWORD:16112005}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true
    validate-on-migrate: false
    ignore-migration-patterns: "*:mismatch,*:missing"
supabase:
  url: dummy
  key: dummy
  bucket: planbookai-storage
  s3:
    endpoint: https://example.com
    region: ap-southeast-1
    accessKey: dummy
    secretKey: dummy
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: alpha
    tags-sorter: alpha

gemini:
  api-key: ${GEMINI_API_KEY:}
  model: gemini-2.0-flash

server:
  port: 8080
